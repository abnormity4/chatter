const ChannelLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className='h-screen bg-amber-50 font-instrument-sans'>{children}</div>
  );
};

export default ChannelLayout;
